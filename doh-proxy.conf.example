# Example DOH-Proxy configuration
# Environment variables override these values.

listen_addr=0.0.0.0
listen_port=53
upstream_timeout_ms=2500
upstream_pool_size=64
cache_capacity=1024
metrics_enabled=1
metrics_port=9090
upstreams=https://cloudflare-dns.com/dns-query,https://dns.google/dns-query
